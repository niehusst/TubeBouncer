{
  "name": "tubebouncer",
  "description": "dev release automation",
  "scripts": {
    "lint": "eslint --fix && web-ext lint --firefox-preview",
    "start": "web-ext run --firefox-preview",
    "test": "ava **/*.test.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "web-ext": "^7.3.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.4.0",
    "ava": "^6.1.3",
    "eslint": "^9.4.0",
    "globals": "^15.4.0"
  }
}
